telemetry = false

[install]
# Install exact version defined in package.json even if a range is specified
# exact = true
# Only install package versions published at least 7 days ago
# minimumReleaseAge = 604800 # seconds
# Exclude trusted packages from the age gate
minimumReleaseAgeExcludes = ["@types/node", "typescript", "@types/bun"]

[console]
depth = 4
